package net.canarymod.bansystem;

/**
 * The enum of ban types
 *
 * @author Jamie (jamierocks)
 */
public enum BanType {
    /**
     * A ban applied to a UUID
     */
    UUID,

    /**
     * A ban applied to an IP
     */
    IP
}
